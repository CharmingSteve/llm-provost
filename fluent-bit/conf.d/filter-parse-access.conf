[FILTER]
    Name                  parser
    Match                 provost.access
    Key_Name              message
    Parser                json
    Reserve_Data          On
    Preserve_Key          On
